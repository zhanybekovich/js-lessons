"use strict";

let hasDriversLicense = false;
const passTest = true;

if (passTest) {
  hasDriverLicense = true; // without strict mode this code will not cause errors
  console.log("I can drive");
}

const interface = "Hello";
console.log(interface);
