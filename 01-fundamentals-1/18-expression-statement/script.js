// expressions --> produce values
2 + 1;
true && false && true;

// statements --> do not produce values
if (23 > 10) {
  const str = "23 is bigger than 10";
}

// !!! in template literals use only expressions
